name: Deploy backend to Cloud Run

on:
  push:
    branches: [main]
    paths:
      - "backend-fastapi/**"
      - ".github/workflows/deploy-backend-cloud-run.yml"
  workflow_dispatch:

env:
  SERVICE_NAME: backend-fastapi
  REGION: us-central1
  PROJECT_ID: briefed-42540
  CLOUD_SQL_INSTANCE: briefed-42540:us-central1:briefed-db

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Run backend tests
        working-directory: backend-fastapi
        run: uv sync --extra dev && uv run pytest tests -v

      # Requires repo secret GCP_SA_KEY (full JSON key). Fork PRs do not get secrets.
      - name: Ensure GCP_SA_KEY secret is set
        shell: bash
        env:
          GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
        run: |
          if [ -z "$GCP_SA_KEY" ]; then
            echo "::error::Add repository secret GCP_SA_KEY (JSON key for a GCP service account with Cloud Run + Cloud Build deploy permissions)."
            exit 1
          fi

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy ${{ env.SERVICE_NAME }} \
            --source=./backend-fastapi \
            --region=${{ env.REGION }} \
            --project=${{ env.PROJECT_ID }} \
            --allow-unauthenticated \
            --add-cloudsql-instances=${{ env.CLOUD_SQL_INSTANCE }} \
            --update-env-vars=GCP_PROJECT=${{ env.PROJECT_ID }},GCP_LOCATION=${{ env.REGION }},CLOUD_SQL_CONNECTION_NAME=${{ env.CLOUD_SQL_INSTANCE }},DB_NAME=briefed,DB_USER=postgres,SCREENSHOTS_BUCKET=briefed-42540-screenshots \
            --update-secrets=DB_PASS=briefed-db-password:latest \
            --quiet
